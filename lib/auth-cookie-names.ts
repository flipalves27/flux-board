/** Cookie httpOnly com JWT de acesso (curta duração). */
export const ACCESS_COOKIE = "flux_access";
/** Cookie httpOnly com refresh opaco (rotação no servidor). */
export const REFRESH_COOKIE = "flux_refresh";
