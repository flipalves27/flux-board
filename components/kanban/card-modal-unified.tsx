/**
 * Ponto de importação preferido para o modal de card (Onda 4 — consolidação incremental).
 * O comportamento canónico continua em `card-modal.tsx` / `card-modal-context.tsx`.
 */
export { CardModal, type CardModalProps, type CardModalTabId } from "./card-modal";
